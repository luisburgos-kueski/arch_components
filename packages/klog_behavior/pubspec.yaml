name: klog_behavior
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  core:
    path: '../core'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
